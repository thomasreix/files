extendsyntax json formatter prettier --parser json
extendsyntax html formatter prettier --parser html
extendsyntax css formatter prettier --parser css
EOF

echo -e '\e[6 q' >> ~/.bashrc
source ~/.bashrc
sudo mkdir -p /etc/portage/package.use
echo "x11-terms/st savedconfig" | sudo tee -a /etc/portage/package.use/st
sudo emerge --ask x11-terms/st
sudo nano /etc/portage/savedconfig/x11-terms/st-*
sudo sed -i 's/cursorshape = [0-9]*/cursorshape = 6/' /etc/portage/savedconfig/x11-terms/st-*
sudo emerge --ask x11-terms/st
# 1. Remove Gentoo's version
sudo emerge --deselect x11-terms/st
sudo emerge --depclean x11-terms/st
# 2. Put the source code in your clean .config folder
git clone https://git.suckless.org/st ~/.config/st
cd ~/.config/st
# 3. Edit your settings and compile it yourself
nano config.def.h
sudo make clean install
# 1. Remove the savedconfig rule
sudo rm /etc/portage/package.use/st
# 2. Delete the vanilla config file I made you generate
sudo rm -rf /etc/portage/savedconfig/x11-terms/st-*
# 3. Recompile st (this will restore your original config/patches!)
sudo emerge --ask x11-terms/st
echo "printf '\033[6 q'" >> ~/.bashrc
source ~/.bashrc
# 1. Make sure savedconfig is turned on
echo "x11-terms/st savedconfig" | sudo tee /etc/portage/package.use/st
# 2. Tell Portage to generate the file so we can edit it
sudo emerge --oneshot x11-terms/st
# 3. Find the exact name of the file Gentoo just generated
CONFIG_FILE=$(ls /etc/portage/savedconfig/x11-terms/st-* | head -n 1)
# 4. Inject your Font (Iosevka)
sudo sed -i 's/static char \*font = .*/static char \*font = "Iosevka:pixelsize=14:antialias=true:autohint=true";/' "$CONFIG_FILE"
# 5. Inject your Foreground (#ffffff) - st uses index 258 for default FG
sudo sed -i 's/\[258\] = .*/\[258\] = "#ffffff",/' "$CONFIG_FILE"
# 6. Inject your Background (#0f0f0f) - st uses index 259 for default BG
sudo sed -i 's/\[259\] = .*/\[259\] = "#0f0f0f",/' "$CONFIG_FILE"
# 7. Set the cursor to a straight line
sudo sed -i 's/static unsigned int cursorshape = [0-9]*/static unsigned int cursorshape = 6;/' "$CONFIG_FILE"
# 8. Rebuild st with your custom colors and font
sudo emerge --ask x11-terms/st
nano ~/.bashrc
sudo emerge --deselect x11-terms/st
sudo emerge --depclean x11-terms/st
sudo rm -f /etc/portage/package.use/st
sudo rm -rf /etc/portage/savedconfig/x11-terms/st-*
cd ~/.config/st
sudo make clean install
cd ~/.config/st
nano config.h
ls
# 1. Remove the broken cursor commands from your .bashrc
sed -i '/\\e\[6 q/d' ~/.bashrc
sed -i '/\\033\[6 q/d' ~/.bashrc
# 2. Force-remove the Gentoo package manager version of st that I messed up
sudo emerge --deselect x11-terms/st 2>/dev/null
sudo emerge --depclean x11-terms/st
# 3. Delete the Gentoo savedconfig files I made you create
sudo rm -f /etc/portage/package.use/st
sudo rm -rf /etc/portage/savedconfig/x11-terms/st*
# 4. Go to YOUR original source folder and revert the cursor shape back to normal
cd ~/.config/st
sed -i 's/cursorshape = 6/cursorshape = 2/g' config.h 2>/dev/null
sed -i 's/cursorshape = 6/cursorshape = 2/g' config.def.h 2>/dev/null
# 5. Rebuild your original terminal exactly how it was
sudo make clean install
lsblk
sudo umount /dev/sda1
sudo wipefs --all --force /dev/sda
sudo parted /dev/sda --script mklabel msdos mkpart primary fat32 1MiB 100%
sudo mkfs.vfat -F 32 -n "USB" /dev/sda1
sudo emerge --ask sys-fs/dosfstools
sudo mkfs.vfat -F 32 -n "USB" /dev/sda1
ls /run/media/
ls /run/media/beglary/
lsblk
lsblk
lsblk
lsblk
nether 8 8
[200~cd ~/.config/st
nano ~/.config/st/config.h
grep config.h
cat config.h
ls -a
cd .config/
ls
ls -a
mkdir -p ~/.config/bash
mv ~/.bashrc ~/.config/bash/bashrc
echo 'source ~/.config/bash/bashrc' > ~/.bashrc
# Move the file if it exists
[ -f ~/.bash_profile ] && mv ~/.bash_profile ~/.config/bash/bash_profile
# Create the redirect stub
echo 'source ~/.config/bash/bash_profile' > ~/.bash_profile
cd ~
ls -a
rm -f ~/.bashrc
sudo tee -a /etc/bash/bashrc > /dev/null << 'EOF'

# Load custom .config bashrc to keep home directory clean
if [ "$USER" = "beglary" ] && [ -f ~/.config/bash/bashrc ]; then
    source ~/.config/bash/bashrc
fi
EOF

ls -a
# Create the necessary folders
mkdir -p ~/.config/bash ~/.config/X11
# 1. Move .bash_history and tell bash where it is now
mv ~/.bash_history ~/.config/bash/bash_history 2>/dev/null
echo 'export HISTFILE="$HOME/.config/bash/bash_history"' >> ~/.config/bash/bashrc
# 2. Move .bash_logout and create an exit trap to run it
mv ~/.bash_logout ~/.config/bash/bash_logout 2>/dev/null
echo 'trap "[ -f ~/.config/bash/bash_logout ] && source ~/.config/bash/bash_logout" EXIT' >> ~/.config/bash/bash_profile
# 3. Move .xinitrc and alias 'startx' to always point to the new location
mv ~/.xinitrc ~/.config/X11/xinitrc 2>/dev/null
echo 'alias startx='\''startx "$HOME/.config/X11/xinitrc"'\''' >> ~/.config/bash/bashrc
# 4. Move .bash_profile
mv ~/.bash_profile ~/.config/bash/bash_profile 2>/dev/null
# 5. Tell Gentoo to load your new profile automatically on login
sudo tee /etc/profile.d/clean_home_profile.sh > /dev/null << 'EOF'
if [ "$USER" = "beglary" ] && [ -f ~/.config/bash/bash_profile ]; then
    source ~/.config/bash/bash_profile
fi
EOF

# 6. Delete the old files from your home directory to be absolutely sure
rm -f ~/.bash_history ~/.bash_logout ~/.bash_profile ~/.xinitrc
ls -a
cd .config/
ls -a
nano bash/bashrc 
source ~/.config/bash/bashrc 
nano bash/bashrc 
cd ~/.config/st
nano config.h
sudo make clean install
nano ~/.config/st/config.h
sudo make clean install
cd ~
st
cd ~/.config/st
cp config.def.h config.h
beglary@gentoo ~ $ st
Segmentation fault         st
beglary@gentoo ~ $
fc-list :family | grep -i iosevka
cd ~/.config/st
# 1. Set the Font (adjusting name to "Iosevka" for safety)
sed -i 's/static char \*font = .*/static char \*font = "Iosevka:pixelsize=14:antialias=true:autohint=true";/' config.h
# 2. Set Background color in slot [0]
sed -i 's/\[0\] = "#000000",/\[0\] = "#0f0f0f",/' config.h
# 3. Set Foreground color in slot [7]
sed -i 's/\[7\] = "#bbbbbb",/\[7\] = "#ffffff",/' config.h
# 4. Ensure st uses index 7 for text and index 0 for background
sed -i 's/unsigned int defaultfg = .*/unsigned int defaultfg = 7;/' config.h
sed -i 's/unsigned int defaultbg = .*/unsigned int defaultbg = 0;/' config.h
# 5. Set the cursor to a straight line
sed -i 's/static unsigned int cursorshape = .*/static unsigned int cursorshape = 6;/' config.h
# 6. Compile
sudo make clean install
sy
st
n config.h
nano config.h
[200~sudo make clean install
~sudo make clean install
sudo make clean install
sudo make clean install
st
nano .config/st/config.h
nano config.h
cd ~/.config/st
nano config.h
kitty
cd .config/bash/
nano bashrc 
source .bashrc
source ~/.bashrc
m bashrc 
nano .bashrc 
[200~~/.bashrc -> ~/.config/bash/bashrc
~/.bash_profile -> ~/.config/bash/bash_profile
~/.bash_logout -> ~/.config/bash/bash_logout~
~/.bashrc -> ~/.config/bash/bashrc
~/.bash_profile -> ~/.config/bash/bash_profile
~/.bash_logout -> ~/.config/bash/bash_logout
ln -s ~/.config/bash/bashrc ~/.bashrc
ln -s ~/.config/bash/bash_profile ~/.bash_profile
ln -s ~/.config/bash/bash_logout ~/.bash_logout
ls -a
nano .bash_logout 
nano .bash_logout 
[200~ln -s ~/.config/bash/bashrc ~/.bashrc
ln -s ~/.config/bash/bash_profile ~/.bash_profile
ln -s ~/.config/bash/bash_logout ~/.bash_logout~
ln -s ~/.config/bash/bashrc ~/.bashrc
ln -s ~/.config/bash/bash_profile ~/.bash_profile
ln -s ~/.config/bash/bash_logout ~/.bash_logout
rm -rf .bash*
ls -a
[200~ln -s ~/.config/bash/bashrc ~/.bashrc
ln -s ~/.config/bash/bash_profile ~/.bash_profile
ln -s ~/.config/bash/bash_logout ~/.bash_logout~
ln -s ~/.config/bash/bashrc ~/.bashrc
ln -s ~/.config/bash/bash_profile ~/.bash_profile
ln -s ~/.config/bash/bash_logout ~/.bash_logout
ln -s ~/.config/bash/bashrc ~/.bashrc
ln -s ~/.config/bash/bash_profile ~/.bash_profile
ln -s ~/.config/bash/bash_logout ~/.bash_logout
yazi
yazi
yazi
[200~ls -la ~/.bashrc ~/.bash_pro
ls -la ~/.bashrc ~/.bash_profile ~/.bash_logout
source .bashrc\
source .bashrc
triangles  
nether 8 8 
cd programming/c/
e
e program.c
source ~/.bashrc 
e program.c
e program.c
m program
cd .config/nano/
ls
nano nanorc 
cd ../micro
ls
cd colorschemes/
ls
m
m .config/hypr/hyprland.lua 
audacity
flatpak install flathub org.famistudio.FamiStudio
sudo flatpak install flathub org.famistudio.FamiStudio
ffmpeg -hh
ffmpeg -h
cd Downloads/
ffplay farex-pull-my-pearl.mp3 
sudo emerge media-video/mpv
mpv farex-pull-my-pearl.mp3 
ffmpeg -i farex-pull-my-pearl.mp3 -t 10 first-10-seconds.mp3
ls
mpv first-10-seconds.mp3 
m .config/hypr/hyprland.lua 
ffmpeg -i farex-pull-my-pearl.mp3 -t 12 first-10-seconds.mp3
ffmpeg -i -p farex-pull-my-pearl.mp3 -t 12 first-10-seconds.mp3
cd Downloads/
ffmpeg -i farex-pull-my-pearl.mp3 -t 12 first-10-seconds.mp3
mpv first-10-seconds.mp3 
ffmpeg -i farex-pull-my-pearl.mp3 -t 15 first-10-seconds.mp3
mpv first-10-seconds.mp3 
ffmpeg -i farex-pull-my-pearl.mp3 -t 14 first-10-seconds.mp3
mpv first-10-seconds.mp3 
ffmpeg -i farex-pull-my-pearl.mp3 -t 13 first-10-seconds.mp3
mpv first-10-seconds.mp3 
ffmpeg -i farex-pull-my-pearl.mp3 -t 13.2 first-10-seconds.mp3
mpv first-10-seconds.mp3 
ffmpeg -i farex-pull-my-pearl.mp3 -t 13.5 first-10-seconds.mp3
mpv first-10-seconds.mp3 
ffmpeg -i farex-pull-my-pearl.mp3 -t 13.1 first-10-seconds.mp3
mpv first-10-seconds.mp3 
mpv first-17-seconds.mp3 
ffmpeg -i farex-pull-my-pearl.mp3 -t 18 first-10-seconds.mp3
mpv first-10-seconds.mp3 
ffmpeg -i farex-pull-my-pearl.mp3 -t 15 first-10-seconds.mp3
mpv first-10-seconds.mp3 
mpv first-10-seconds.mp3 
ffmpeg -i farex-pull-my-pearl.mp3 -t 16 first-10-seconds.mp3
mpv first-10-seconds.mp3 
ffmpeg -i farex-pull-my-pearl.mp3 -t 15 farex-pull-my-pearl_15.mp3
mpv farex-pull-my-pearl_15.mp3 
ffmpeg -i farex-pull-my-pearl.mp3 -t 10 farex-pull-my-pearl_10.mp3
mpv farex-pull-my-pearl_10.mp3 
ffmpeg -i farex-pull-my-pearl.mp3 -t 11 farex-pull-my-pearl_10.mp3
mpv farex-pull-my-pearl_10.mp3 
ffmpeg -i farex-pull-my-pearl.mp3 -t 12 farex-pull-my-pearl_10.mp3
mpv farex-pull-my-pearl_10.mp3 
ffmpeg -i farex-pull-my-pearl.mp3 -t 11 farex-pull-my-pearl_11s.mp3
ffmpeg -i farex-pull-my-pearl.mp3 -t 15 farex-pull-my-pearl_15s.mp3
ls
rm farex-pull-my-pearl_10.mp3 
rm farex-pull-my-pearl_15.mp3 
ls
rm first-10-seconds.mp3 
ls
emerge --ask kde-apps/dolphin
dolphin
emerge --ask x11-libs/xcb-util-cursor
dolphin
exit
m .config/hypr/hyprland.lua 
cd .config/
ls
cat dunst/dunstrc 
yazi
cd .config/micro/
ls
cd backups/
ls
m %2Fhome%2Fbeglary%2F.config%2Fdunst%2Fdunstrc 
cd ..
ls
rm -rf backups/
ls
cd colorschemes/
ls
cat config.sh 
cd ..
ls
cd buffers/
ls
cat history 
cd ..
ls
rm -rf buffers/
ls
m bindings.json 
m settings.json.json 
m settings.json
sudo emerge micro
cd
cd .config/
ls
cd micro/
m bindings.json 
m bindings.json 
m bindings.json 
m bindings.json 
m .config/foot/foot.ini 
[main]
[main]
[main]
\x03
cd programming/c/
m program.c 
cd .config/foot/
m foot.ini 
m
cd programming/c/
m program.c 
m program.c 
m ~/.config/micro/settings.json 
cat ~/.config/micro/settings.json
foot --version
micro --version
dolphin
su
dolphin
start-hyprland
start-hyprland
exit
start-hyprland
start-hyprland
exit
emerge vesktop
emerge net-im/vesktop-bin
exit
vesktop
vesktop-bin
su
vesktop
vesktop-bin
vesktop
echo "DISPLAY=$DISPLAY"
echo "WAYLAND_DISPLAY=$WAYLAND_DISPLAY"
echo "XDG_SESSION_TYPE=$XDG_SESSION_TYPE"
echo "DBUS_SESSION_BUS_ADDRESS=$DBUS_SESSION_BUS_ADDRESS"
loginctl show-session "$XDG_SESSION_ID" -p Type -p Display -p Remote
echo "WAYLAND_DISPLAY=$WAYLAND_DISPLAY"
echo "DISPLAY=$DISPLAY"
echo "XDG_RUNTIME_DIR=$XDG_RUNTIME_DIR"
echo "DBUS_SESSION_BUS_ADDRESS=$DBUS_SESSION_BUS_ADDRESS"
ls -l "$XDG_RUNTIME_DIR"/wayland-*
ls -l "$XDG_RUNTIME_DIR/bus"
env | grep -E '^(WAYLAND|DISPLAY|XDG|DBUS)'
ls -l "$XDG_RUNTIME_DIR/bus"
systemctl --user status dbus
vesktop
ls -l /run/user/1000/bus
eval "$(dbus-launch --sh-syntax)"
echo "$DBUS_SESSION_BUS_ADDRESS"
vesktop
gtk-launch
GDK_BACKEND=wayland gtk-launch
GDK_BACKEND=x11 gtk-launch
zenity --info --text="GTK test"
vesktop --ozone-platform=x11
sudo emerge --ask app-misc/cliphist
sudo emerge --ask gui-apps/wl-clipboard
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store
micro ~/.config/hypr/hyprland.conf
head -30 ~/.config/hypr/hyprland.lua
micro ~/.config/hypr/hyprland.lua 
micro ~/.config/hypr/hyprland.lua 
hyprctl reload
cliphist list
sudo emerge --ask app-misc/cliphist
sudo mkdir -p /etc/portage/package.accept_keywords
echo "app-misc/cliphist ~amd64" | sudo tee /etc/portage/package.accept_keywords/cliphist
sudo emerge --ask app-misc/cliphist
cliphist --versio
cliphist --version
cliphist
which wl-paste
cliphist list
wl-paste --type text --watch cliphist store
echo "hello clipboard" | wl-copy
cliphist list
cd Downloads/
unzip survieTR.zip 
echo "=== SESSION ==="
echo "$XDG_SESSION_TYPE"
echo "$WAYLAND_DISPLAY"
echo "$DISPLAY"
echo "=== GPU ==="
lspci | grep -Ei 'vga|3d|display'
echo "=== OPENGL ==="
glxinfo -B 2>&1 | head -30
echo "=== VULKAN ==="
vulkaninfo --summary 2>&1 | head -80
eselect opengl list
emerge --info | grep -E 'VIDEO_CARDS|USE='
ls -l /usr/share/vulkan/icd.d/
ls -l /usr/share/vulkan/
ldconfig -p | grep -E 'libEGL|libGLX|libGL\.so'
ls -l /usr/lib64/libnvidia-egl-wayland.so*
equery list '*nvidia*'
qlist -Iv | grep -i nvidia
equery u x11-drivers/nvidia-drivers
emerge -pv x11-drivers/nvidia-drivers
sudo mkdir -p /etc/portage/package.use
echo "x11-drivers/nvidia-drivers wayland" | sudo tee /etc/portage/package.use/nvidia
sudo emerge -avuDN @world
m
y
m .bashrc 
y
source .bashrc 
y
sudo emerge yazi
vesktop
vulkaninfo --summary 2>&1 | head -80
vesktop --ozone-platform=x11
vesktop --ozone-platform=x11
m ~/programming/voxel.py 
cd programming/
python voxel.py 
[A
python voxel.py 
python voxel.py 
python voxel.py 
python voxel.py 
python voxel.py 
python voxel.py 
python voxel.py 
m .config/micro/bindings.json 
m .config/session/
m .config/micro/settings.json 
m .config/micro/settings.json 
m .config/micro/bindings.json 
m .config/micro/init.lua
m .config/micro/init.lua
vesktop --ozone-platform=x11
m .config/hypr/hyprland.lua 
cd programming/c/
c program.c
c program.c
c program.c
c program.c
c program.c
c program.c
c program.c
c program.c
c program.c
c program.c
c program.c
c program.c
j
c program.c
c program.c
c program.c
c program.c
c program.c
source .bashrc
source ~/.bashrc
c program.c
m .bashrc 
source .bashrc 
ls -a
